<form class="site-search hds-text-input" action="{{ path('view.search.page_1') }}" method="get" accept-charset="UTF-8" role="search">
  <input class="site-search__field hds-text-input__input" type="text" placeholder="{{ 'Search…'|t }}" aria-label="{{ 'Search for'|t }}" name="search">

  <button class="site-search__button" type="submit">
    <i class="hds-icon hds-icon--search hds-icon--size-s" role="presentation"></i>
    <span class="visually-hidden">{{ 'Search' | t }}</span>
  </button>
</form>
